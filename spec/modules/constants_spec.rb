require 'spec_helper'

RSpec.describe Constants do
  describe 'module constants are exists' do
    # it { expect(ConsoleInterface::DIFFICULTY_ORDER).to be_truthy }
    # it { expect(ConsoleInterface::TABLE_HEADER).to be_truthy }
    # it { expect(ConsoleInterface::START).to be_truthy }
    # it { expect(ConsoleInterface::RULES).to be_truthy }
    # it { expect(ConsoleInterface::STATS).to be_truthy }
    # it { expect(ConsoleInterface::EXIT).to be_truthy }
    # it { expect(ConsoleInterface::HINT).to be_truthy }
    # it { expect(ConsoleInterface::YES).to be_truthy }
  end
end
